class API {
  private _baseURL: string;

  constructor(baseURL: string) {
    this._baseURL = baseURL;
  }

  // async request(route: string, params: Object):  {

  // }
}

// eslint-disable-next-line import/prefer-default-export
export { API };
